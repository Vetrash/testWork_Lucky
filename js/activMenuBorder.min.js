const menuItems=document.querySelectorAll(".menu li"),borders=document.querySelectorAll(".orangeBorder"),addBorder=e=>{const r=e.target.nextSibling;borders.forEach((e=>{e.classList.remove("activDisplay")})),r.classList.add("activDisplay")};menuItems.forEach((e=>{e.addEventListener("click",addBorder)}));